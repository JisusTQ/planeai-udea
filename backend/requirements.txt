# ============================================================
#  Dependencias del backend de PlaneAI UdeA
#  Instalar con:  pip install -r requirements.txt
#  (Requiere Python 3.13)
# ============================================================

# ----- Framework web (API REST) -----
fastapi==0.115.5
uvicorn[standard]==0.32.1        # Servidor ASGI para desarrollo y producción

# ----- Configuración y validación -----
pydantic==2.10.3                 # Validación de datos / esquemas
pydantic-settings==2.7.0         # Lectura tipada de variables de entorno (.env)

# ----- Inteligencia Artificial -----
google-generativeai==0.8.3       # SDK oficial de Google Gemini

# ----- Consulta de datos en vivo (portales UdeA) -----
requests==2.32.3                 # Peticiones HTTP a los portales oficiales

# ----- Testing / verificación -----
httpx==0.28.1                    # Cliente HTTP para TestClient (smoke tests)
