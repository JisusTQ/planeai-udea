"""
Paquete de esquemas Pydantic (validación de entrada/salida de la API).

Aquí vivirán los DTOs (Data Transfer Objects) que definen qué datos entran y
salen por cada endpoint, separando el modelo de la BD (SQLAlchemy) del contrato
público de la API. Se implementarán en la fase de endpoints.
"""
